name: build

on:
    push:
        branches: [ master ]
    pull_request:
        branches: [ master ]


jobs:
    devices:
        uses: tayloraswift/swift-ci/.github/workflows/device.yml@master
        with:
            xcode-scheme: "swift-ip-Package"
